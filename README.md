# embedded-ci-lab

A minimal local Python CLI that reads a YAML pipeline, executes steps sequentially, and generates a JSON report.

## MVP Features
- Python CLI interface.
- YAML pipeline definitions.
- Sequential command execution.
- JSON reporting.

## Installation
```bash
pip install -e .[dev]
```

## Usage
```bash
embedded-ci --help
```
